# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# rsconnect deployment metadata (account/app records, not needed on GitHub)
rsconnect/

# OS / editor
.DS_Store
Thumbs.db

# Originaldatensatz -- gehoert NICHT ins Repo.
# Die App laeuft ausschliesslich auf data/app_data.rds (vorberechnete Ergebnisse
# aus build.R). Der Datensatz selbst wird nicht veroeffentlicht; build.R liest
# ihn von ausserhalb des Repos (Pfad in model.R bzw. Umgebungsvariable
# THESIS_DATA).
*.xlsx
*.xls
data/Datensatz_*
